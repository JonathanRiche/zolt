
## Notes on TODO.md
- Track core tooling gaps like `exec_command` + `write_stdin` and discovery helpers (`grep_files`, `read_file`, `list_dir`).
- UX control items (`update_plan`, `request_user_input`) are still pending.
- External context tooling (MCP resource listing/reading, optional `web_search`, `view_image`) remains TODO.
- Advanced runtime options (JS REPL, multi-agent controls) are listed for later evaluation.